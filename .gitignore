node_modules/
dist/
.env
*.log
mcp-server/node_modules/
mcp-server/dist/
ingest/__pycache__/
ingest/.venv/
__pycache__/

# Local config with secrets
config/devbrain.yaml
!config/devbrain.yaml.example

# Credentials
.devbrain/credentials/

# Per-dev profile dirs — created at runtime by `devbrain login`, contain
# Library/Keychains/login.keychain-db (per-dev OAuth tokens), .gitconfig,
# and Claude/Codex/Gemini state. MUST be ignored: factory/readiness.py runs
# `git clean -fd` between jobs and would otherwise destroy them.
profiles/

# Git worktrees — used for isolated feature work (using-git-worktrees skill)
.worktrees/
/onboarding/
backups/
